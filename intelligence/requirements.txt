# Phase 1 — ingestion, chunking, embed + store
# NOTE: Docker image on Railway was timing out because
# unstructured[pdf] pulls in huge GPU / torch dependencies.
# For the deployed admin app we use the lighter core package.
# Install with: pip install -r requirements.txt

unstructured>=0.16.0
pdfminer.six>=20260107
pi-heif>=1.3.0
tiktoken>=0.5.0
chromadb>=0.4.0
openai>=1.0.0
anthropic>=0.39.0
pymupdf>=1.24.0

