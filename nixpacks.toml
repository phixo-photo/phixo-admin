[phases.setup]
nixPkgs = ["nodejs_20", "python311"]

[phases.install]
cmds = [
  "npm install",
  "pip install -r intelligence/requirements.txt"
]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
