[phases.setup]
nixPkgs = ["ffmpeg", "nodejs_20", "python3", "curl"]

[phases.install]
cmds = [
  "npm install",
  "curl -L https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp",
  "chmod a+rx /usr/local/bin/yt-dlp",
  "yt-dlp --version"
]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
